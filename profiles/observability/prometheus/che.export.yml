# yaml-language-server: $schema=https://gitlab.com/api/v4/projects/konradodwrot%2Fgo-modules/packages/generic/che-schema/v0.0.1-che-min-v0.0.96/che.schema.json
##[>] 🤖🤖
options:
  profileWorkingDirectory: root

observability/prometheus:
  options:
    runIf:
      - builtin:isOs == macos
      - builtin:isVirt == false
  include:
    makeLinks:
      - etc/**
    makeCopies:
      - owner: root
        ownerGroup: "0"
        chmod: "0644"
        source: Library/LaunchDaemons/prometheus.plist.ontoHost.cp
        dest:
          - /Library/LaunchDaemons/prometheus.plist
    makeDirs:
      - chmod: "2775"
        directories:
          - dest:
              - /var/log/prometheus
          - dest:
              - /usr/local/var/prometheus
    installPackages:
      - prometheus
    runScripts:
      - scripts/01-boot-service.zsh
##[<] 🤖🤖
