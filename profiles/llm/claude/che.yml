# yaml-language-server: $schema=https://gitlab.com/konradodwrot/unrestricted/go-modules/-/raw/main/che/assets/data/che.schema.json
##[>] 🤖🤖
options:
  profileWorkingDirectory: root

llm/claude:
  include:
    makeLinks:
      - {source: _home/**, dest: $HOME/**}
    renderTemplates:
      - templates: [{source: _home/**, dest: $HOME/**}]
    runScripts:
      - scripts/01-install-claude.zsh
      - scripts/02-install-completions.zsh

llm/claude/virt:
  options: {profileWorkingDirectory: .}
  include:
    renderTemplates:
      - ctx: {repo: Configs}
        templates:
          - {source: templates/snippets/ro.md, dest: [{path: "${invokingSpecGitRoot}/.claude/agents/ro.md", options: {skipAutoGeneratedHeader: true}}]}
          - {source: templates/snippets/rw.md, dest: [{path: "${invokingSpecGitRoot}/.claude/agents/rw.md", options: {skipAutoGeneratedHeader: true}}]}
          - {source: templates/snippets/settings.json, dest: [{path: "${invokingSpecGitRoot}/.claude/settings.json", options: {skipAutoGeneratedHeader: true}}]}
          - {source: templates/snippets/claude-gitignore, dest: ["${invokingSpecGitRoot}/.claude/.gitignore"]}
##[<] 🤖🤖
