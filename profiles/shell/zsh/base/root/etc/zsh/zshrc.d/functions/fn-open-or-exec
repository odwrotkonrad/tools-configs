##[>] 🤖🤖
function fn-open-or-exec {
  emulate -LR zsh
  local opener=$1 cmd=$2
  shift
  if (( $+commands[$cmd] )) {
    "$@"
  } elif [[ -x $cmd && ! -d $cmd ]] {
    "${cmd:a}" "${@:2}"
  } else {
    local -a opener_argv=( ${(Q)${(z)opener}} )
    "${opener_argv[@]}" "$@"
  }
}
##[<] 🤖🤖
