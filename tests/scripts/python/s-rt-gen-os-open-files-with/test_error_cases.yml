- name: too_many_args
  args: [one, two]
  exit: 2

- name: missing_config
  args: [/nonexistent/path/to/config.yml]
  exit: 10

- name: bad_config
  args: []
  bad_config: "handlers: [unclosed\n"
  exit: 11
