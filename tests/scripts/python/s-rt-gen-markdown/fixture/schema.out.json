{
  "$defs": {
    "Arguments": {
      "description": "The validated positional arguments.",
      "properties": {
        "template": {
          "$ref": "#/$defs/TemplatePathStr"
        }
      },
      "required": [
        "template"
      ],
      "title": "Arguments",
      "type": "object"
    },
    "ScriptBaseOptions": {
      "description": "Options every script carries; scripts subclass to add their own.",
      "properties": {
        "help": {
          "default": false,
          "title": "Help",
          "type": "boolean"
        },
        "json_schema": {
          "default": false,
          "title": "Json Schema",
          "type": "boolean"
        }
      },
      "title": "ScriptBaseOptions",
      "type": "object"
    },
    "TemplatePathStr": {
      "type": "string"
    }
  },
  "description": "The validated invocation: arguments, options, and the input->action grammar.",
  "properties": {
    "arguments": {
      "$ref": "#/$defs/Arguments"
    },
    "options": {
      "$ref": "#/$defs/ScriptBaseOptions",
      "default": {
        "help": false,
        "json_schema": false
      }
    }
  },
  "required": [
    "arguments"
  ],
  "title": "Parameters",
  "type": "object"
}