#[≟] Repository directory tree. Autogenerated by root_ln/usr/local/scripts/shell/s-rt-generate-yaml-dir-tree.
docs/:
  - images
  - recordings
root_cp/:
  - Library:
      - LaunchDaemons
  - etc:
      - sudoers.d
root_ln/:
  - Users:
      - ko:
          - .config:
              - asdf
              - claude:
                  - agent-memory
                  - agents
                  - commands
                  - output-styles
                  - plugins:
                      - marketplaces
                  - rules:
                      - config:
                          - zsh
                  - skills
                  - themes
              - custom
              - direnv
              - git
              - homebrew
              - kitty
              - lefthook
              - prettier
              - ruff
              - tmux
              - vim
              - zsh:
                  - completions
                  - functions
                  - rc.d
          - .ssh
          - Library:
              - Application Support:
                  - Code:
                      - User:
                          - .exported
              - LaunchAgents
  - etc:
      - custom
      - grafana:
          - provisioning:
              - dashboards:
                  - json
              - datasources
      - jaeger
      - loki
      - otelcol
      - prometheus
      - python
      - rg
      - zsh:
          - completions
          - functions
          - rc.d
  - usr:
      - local:
          - scripts:
              - installs
              - python
              - shell
tests/:
  - scripts:
      - python:
          - s-rt-auth:
              - mock
