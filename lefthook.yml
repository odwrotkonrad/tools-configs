##[>] 🤖
extends:
  - ~/.config/lefthook/lefthook.yml

pre-commit:
  jobs:
    - name: docsgen
      run: make run-repo-ci-render-templates && git add assets/data/repo-structure.md assets/data/makefile.agents.md README.md CLAUDE.md AGENTS.md

pre-push:
  jobs:
    - name: test-go
      run: make run-repo-ci-tests-go
##[<] 🤖
