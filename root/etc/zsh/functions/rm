#[≟] shadow rm with trash; drop flags, forward paths only
#[∵] trash is recoverable, rm unlinks irreversibly
#[⌖] $ man trash

emulate -LR zsh

local -a paths
local arg
for arg in "$@"; do
  [[ $arg == -* ]] && continue  #[≟] drop flags
  paths+=("$arg")
done

trash "${paths[@]}"
