#[≟] system secret registry: service -> container -> field -> env var (null = not exported).
defaults:
  vault: Personal

services:
  ssh:
    ssh_id_access:
      password: null
    ssh_id_signing:
      password: null
