plugins:
  op:
    cmd: op
    args: [read, --no-newline]
    timeout: 30s
  readMarkdown:
    cmd: read-markdown.zsh
  readWithFrontmatter:
    cmd: read-with-frontmatter.zsh
  generateTree:
    cmd: gen-dirs-tree
  mergeUpsertEnv:
    cmd: merge-upsert-env.zsh
  generateMakefileDoc:
    cmd: tpl-gen-makefile-doc
