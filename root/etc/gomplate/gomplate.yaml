plugins:
  op:
    cmd: op
    args: [read, --no-newline]
    timeout: 30s
  renderMarkdown:
    cmd: render-markdown.zsh
  renderWithFrontmatter:
    cmd: render-with-frontmatter.zsh
  renderDirsTree:
    cmd: render-dirs-tree
  tplRenderMergeUpsertEnv:
    cmd: tpl-render-merge-upsert-env.zsh
  renderMakefileDoc:
    cmd: render-makefile-doc
