plugins:
  op:
    cmd: op
    args: [read, --no-newline]
    timeout: 30s
  readMarkdown:
    cmd: s-root-read-markdown
  readWithFrontmatter:
    cmd: s-root-read-with-frontmatter
  generateTree:
    cmd: s-root-generate-tree
  mergeUpsertEnv:
    cmd: s-root-merge-upsert-env
  generateMakefileDoc:
    cmd: tmpl-gen-makefile-doc
