emulate -L zsh

typeset -a ssh_keys=(
    ~/.ssh/id_access
    ~/.ssh/id_signing
)

for k in ${ssh_keys}; do 
    ssh-keygen -t ecdsa-sha2-nistp521 -b 521 -f ${k} -C 'odwrotkonrad+kodwrot-lt@gmail.com'
done
