plugins:
  op:
    cmd: op
    args: [read, --no-newline]
    timeout: 30s
  readMarkdown:
    cmd: s-rt-read-markdown
  readWithFrontmatter:
    cmd: s-rt-read-with-frontmatter
  generateTree:
    cmd: s-rt-generate-tree
  mergeUpsertEnv:
    cmd: s-rt-merge-upsert-env
