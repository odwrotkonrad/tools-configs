#[≟] print msg then exit code; code 0 -> stderr, nonzero -> stdout

function exit-with {
  emulate -LR zsh

  typeset code=$1 msg=${2}${2:+"\n"}

  if (( code==0 )) print -u2 -n ${msg}
  if (( code!=0 )) print -n ${msg}

  exit $code
}
