## Git Workflow

Use the `ko`-git skills as the primary way to interact with git. Never hand-derive branch names, commit messages, or MR text.

- `/ko-git-branch-name-upsert` — create/rename branch to a conventional name.
- `/ko-git-commit` — commit with a generated message.
- `/ko-git-mr-upsert` — create/update the MR/PR.
- `/ko-git-upsert-all` — run all three in order.
