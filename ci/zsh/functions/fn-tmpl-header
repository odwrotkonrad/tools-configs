#[what] autogen header line for $out (arg1), comment syntax by extension

function fn-tmpl-header {
  local out=$1 template=$2
  if [[ ${out:e} == md ]] {
    print -r -- "<!-- autogenerated using $template -->"
  } else {
    print -r -- "# autogenerated using $template"
  }
}
